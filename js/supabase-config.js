// Fill these in after creating a free project at https://supabase.com.
// The anon key is designed to be public (it's the same one your browser's
// network tab would show regardless); real protection comes from the Row
// Level Security policies in supabase/schema.sql, not from hiding this key.
// Until these are filled in, the Attic and Living Room will show a clear
// "not connected yet" state instead of failing silently.
window.SUKOON_SUPABASE = {
  url: '',      // e.g. https://abcdefghij.supabase.co
  anonKey: '',  // Project Settings > API > anon public key
};
